# 项目协作规则

本文件适用于当前仓库及其所有子目录。

## 沟通语言

- 与用户沟通、说明修改内容和报告结果时，统一使用中文。
- 新增的项目文档、代码注释和 Git 提交说明优先使用简洁、清晰的中文；仅在技术规范或现有代码风格要求英文时使用英文。

## 修改原则

- 修改前先检查仓库状态，保留用户已有但与当前任务无关的改动。
- 只修改当前任务涉及的文件，不擅自删除、覆盖或提交无关文件。
- 完成修改后进行与改动风险相匹配的检查或测试。
- 若检查失败，应先修复问题；无法修复时向用户明确说明，不把失败结果描述为已完成。

## Git 提交与推送

- 每次任务修改完成并通过必要检查后，自动将本次任务涉及的文件提交到 Git。
- 暂存时只添加本次任务涉及的明确文件，不使用 `git add .`、`git add -A` 或 `git add --all`。
- Git 提交信息使用简洁的中文，准确描述本次修改内容。
- 提交成功后，自动推送到当前分支所跟踪的 GitHub 远程仓库；若当前分支尚未设置上游，则推送到 `origin` 的同名分支并设置上游。
- 推送前再次确认提交中不包含用户的无关修改、临时文件、密钥、令牌或其他敏感信息。
- 如果远程仓库认证失败、网络不可用、分支受保护或推送被拒绝，应保留本地提交并用中文告知用户具体原因和下一步处理建议。
- 除非用户明确要求，否则不强制推送、不改写远程历史，也不使用 `--force` 或 `--force-with-lease`。

## 完成报告

- 完成后用中文简要说明修改内容、验证结果、提交哈希和推送状态。
- 若工作区仍有未提交内容，应说明这些内容是否为用户原有改动以及为何未纳入本次提交。
